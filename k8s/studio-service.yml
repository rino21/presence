apiVersion: v1
kind: Service
metadata:
  name: studio-service
  namespace: presence
spec:
  selector:
    app: studio
  type: NodePort
  ports:
    - port: 5555
      targetPort: 5555
      nodePort: 31005